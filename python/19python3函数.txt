Python3函数

***函数是组织好的，可重复使用的，用来实现单一，或相关联功能的代码段；
***函数能提高应用的模块性，和代码的重复利用率；

一、定义一个函数
***可以定义一个由自己想要功能的函数，return 【表达式】结束函数，不带表达式的return相当于返回None；
***语法：
def 函数名（参数列表）：
	函数体

二、函数调用
***函数写好之后，就可以直接使用了；

三、参数传递
***在python中类型属于对象，变量是没有类型的；
***可更改与不可更改对象；string、tuples、numbers属于不可更改的对象；列表、字典、集合属于可更改对象；

四、参数
***必需参数
***关键字参数
***默认参数
***不定长参数

五、匿名函数
***python使用lambda来创建匿名函数；
***lambda 表达式：式子；

六、return语句
***return[表达式]语句用于退出函数，选择性地向调用方返回一个表达式；

七、强制位置参数
